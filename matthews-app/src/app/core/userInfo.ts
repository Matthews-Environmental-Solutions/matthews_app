export interface UserInfo {
  firstName: string;
  lastName: string;
  title: string;
  email: string;
  photoId: string;
  functionId: number;
  function: string;
  departmentId: number;
  department: string;
  name: string;
  id: string
  photoBase64: string;
  imageBlob: Blob;
}
